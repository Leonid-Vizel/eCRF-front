import React from 'react';
import './version.scss';

const Version = () => {
    return (
        <div className="version">
            <span className='version-span'>Версия 0.0.1</span>
        </div>
    );
};

export default Version;